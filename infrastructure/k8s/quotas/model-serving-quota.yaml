apiVersion: v1
kind: ResourceQuota
metadata:
  name: model-serving-quota
  namespace: model-serving
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    pods: "10"
    services: "5"
    persistentvolumeclaims: "3"
