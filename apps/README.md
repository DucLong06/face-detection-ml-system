# apps/

Application source code (mỗi app 1 thư mục con).

## Cấu trúc
- `face-detect/` — FastAPI + YOLOv11 face detection service
  - `src/` mã nguồn · `tests/` test · `Dockerfile` · `docker-compose.yaml`

## Đọc gì tiếp
- CI build: `.github/workflows/` · Deploy: `charts/face-detect/` → `gitops/`
